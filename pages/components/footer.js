import React from "react";

const footer = (props) => {
  return <div className="h-28 shadow-2xl shadow-2xl bg-white w-screen p-4 flex items-center">{props.children}</div>;
};

export default footer;
